## Array of potential keysizes from 2-40
## Convert string from base64 into binary/bytes
## For each potential distance
  # take the first and second keysize worth of bytes and get hamming distance
  # normalize the results by dividing by keysize